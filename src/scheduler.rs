//! Process scheduler
//! Completely Fair Scheduler (CFS) with Real-Time support

use crate::println;

/// Process scheduler
pub struct Scheduler {
    // TODO: Implement
}

impl Scheduler {
    pub fn new() -> Self {
        Scheduler {}
    }
}

/// Initialize scheduler
pub fn init_scheduler() {
    println!("[SCHEDULER] Initializing process scheduler");
    println!("[SCHEDULER] Scheduler ready");
}
